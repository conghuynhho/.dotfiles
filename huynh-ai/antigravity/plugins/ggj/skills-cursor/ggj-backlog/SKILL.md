---
name: ggj-backlog
description: >
  Interact with GGJungle Backlog (issues, projects, wiki, git PRs) via wiki-mcp
  proxy tools named mcp__wiki-mcp__backlog_*.
---

# Backlog (Cursor entry point)

This is a thin Cursor-only wrapper so the `/` picker shows `/ggj-backlog`
(distinguishing it from other tools' plugins) without duplicating the real
instructions.

Only `name` differs from the canonical skill; keep `description` above in
sync with it by hand whenever that file changes.

**Read `../../skills/backlog/SKILL.md` in full now and follow it exactly** —
that file is the single source of truth for this skill's behavior.
